# =============================================================================
# @satsu/app — Environment Configuration
# =============================================================================
# Copy this file to .env.local

# Relayer URL — the app checks this for online status
# Local relay: http://localhost:3100
# Testnet:     https://relay.satsu.network (when deployed)
NEXT_PUBLIC_RELAYER_URL=http://localhost:3100
